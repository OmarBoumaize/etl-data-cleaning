# etl-data-cleaning
ETL data cleaning and transformation project. Cleans and standardizes messy medical datasets, handles missing values, parses concatenated fields with regex, normalizes formats, and enriches data for loading into a structured system
